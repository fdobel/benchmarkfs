from setuptools import setup

setup(
   name='benchmarkfs',
   version='0.1',
   description='Some benchmark functions',
   author='Florian Obeloer',
   packages=['benchmarkfs'],
   install_requires=['numpy'],
)
